# Caddy Reverse Proxy

Central reverse proxy for all self-hosted applications.

## Features
- Automatic HTTPS
- Simple configuration
- Docker-based deployment

## Notes
Apps connect to Caddy via the `proxy` Docker network.
